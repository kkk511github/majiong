# 当前项目状态

## 当前客户端 0.5.9（2026-09-14）

根据用户最新参考图重做秦淮牌桌：完整深木桌框、青绿织纹与原创夜景背景、四家花牌常驻、大型圆形风位盘。保留四家弃牌每排 8 张、固定尺寸和独立间距、贴近桌面底边的手牌与新摸牌间隔。四个风位 20/18 像素，与数字分格，完整桌框通过九宫格背景保留。详见 TABLE-FLOW-0.5.9.md。

最终 41 项布局专项及其余 100 项界面回归全部通过，共 141 项；164 项逻辑/服务检查全部通过。模拟器已实际打开新版牌桌及碰牌响应，APK 与 IPA 已构建、签名及全部资源比对通过。服务器已更新到 0.5.9-11de1b19；发布前 0 真人在线、0 进行中牌桌并备份 SQLite，容器 healthy，公网权限、原生跨域与 WebSocket 校验通过，线上关键源文件及背景资产哈希与交付源码一致。本轮未新增实体 iPhone 或安卓原生运行验收。

保留南京话慢速报牌与动作音、补花只播语音、六条两行三列、每把四家亮牌与分数、10 秒续局、最后整桌汇总、私有听牌提示、10+90 秒与可取消托管。安卓沿用同一正式签名；签名私钥与真实数据库不打入源码包。未知风罚分、比下胡未启用。

## 历史 0.5.7（2026-09-14）

每次从其他页面返回首页、再次点击首页“牌桌”或品牌标识，都会主动请求最新牌桌列表。正常刷新复用现有 WebSocket，同轮未完成刷新合并；返回前台和网络恢复会重新校时并刷新大厅。查询与出牌/准备的确认相互独立。

新增列表 8 秒、握手及心跳回复 10 秒的超时恢复，退役旧连接后独立重连，不等待可能永远不来的 close 事件。旧连接迟到的消息、打开及错误回调不会覆盖新的连接或练习。刷新期间禁用旧空位；未确认的游戏命令不自动重放。

139 项逻辑/服务、111 项完整界面检查全部通过；先失败后通过的针对性检查、12 项专项及最终包/部署证据见 NETWORK-0.5.7.md 与 network-0.5.7-check.json。iOS/安卓 build 17 已构建并验证版本、签名和包内资源；iOS 已安装启动于模拟器。此版本没有修改规则、数据库结构、账号权限或结算。

SafeLink-212 已部署 0.5.7-a5ae6db0，SQLite 备份完成、容器 healthy，公网版本/权限与原生 CORS 检查通过。iOS 模拟器实际验证从玩法页返回首页重新载入牌桌，保留 kkkk 普通账号和声音偏好。源码包干净解压后 npm ci/build 通过，代码与交付版本一致。

## 历史 0.5.6（2026-09-14）

普通账号不显示开桌入口，管理员和获授权账号可开桌。正式 iPhone 模拟器 build 16 已安装并实际查看首页，保留现有普通账号和声音偏好；没有安装到当前不可用的实体 iPhone。

本轮解决两个实际原生问题：横屏注册键盘弹出时四个输入与提交按钮全部可见，系统上一项导航可用；横屏结算第四名及下方记分说明、复制与继续按钮不再裁切。131 项逻辑/服务、109 项完整界面检查通过，原生键盘和结算页面已实际触摸复核。

独立测试版验证音乐/音效各自开关与输出；切后台约 44 秒后返回，音乐无需再触摸网页即可自动恢复。正式资源没有测试探针，测试 App 已移除。真实扬声器听感未获用户确认，真实来电中断未测试。Android APK 已构建并校验签名/资源，但模拟器未成功启动，原生键盘和触摸仍未验证。

SafeLink-212 发布 0.5.6-cc3a9371，容器 healthy。更新前 5 桌等待、0 真人在线，SQLite 已备份（126976 字节、权限 600）；公网版本、身份保护、原生 CORS 和 WebSocket 校验通过。详细证据见 NATIVE-0.5.6.md 与 experience-0.5.6-check.json。源码包干净解压后 npm ci/build 通过，代码与交付版本一致。综合目标尚有上述原生验证边界，不宣称全平台全部完成。

## 历史 0.5.5（2026-09-14）

普通账号首页与大厅不显示开桌按钮；只有管理员及获独立开桌授权的账号显示，收回后立即隐藏并关闭设置。当前回归已验证三种手机尺寸、普通新注册用户和服务端权限约束。

修复 iOS 横屏软键盘遮住房号输入及加入按钮的问题；登录/注册在键盘上方保留提交，输入区可滚动，输入与按钮保留 44 像素高度。键盘收起恢复完整界面，缩放不误判。原生房号输入、错误提示、关闭及个人页昵称输入已实际操作；最终登录/注册高度修正尚待原生触摸复核。

131 项逻辑/服务及最终 105 项界面回归通过，四真实客户端专项连续三次通过。首次全量有一次无堆栈 JSON 异常，重复运行另发现固定房名污染后续测试，已改唯一房名；完整时序如实保存在 KEYBOARD-0.5.5.md 与 experience-0.5.5-check.json，没有把初次失败算成通过。

Ad Hoc IPA、安卓 APK 已通过签名、版本和包内资源检查，源码清洁解压后 npm ci/build 通过。iOS build 15 已在模拟器命令安装启动；正式包不含 QA 探针，独立 QA App 已移除。未安装到物理 iPhone。

SafeLink-212 已部署 0.5.5-5a4cb469，容器 healthy；公网版本、匿名访问保护、原生 CORS 与 WebSocket 身份检查通过。更新前备份 SQLite 126976 字节、权限 600，保留账号/授权/战绩；远程源文件及容器服务哈希均与交付源码一致。

Mac 本轮曾解锁，随后在最终键盘复核时再次锁屏。原生登录/注册键盘、真实音频/中断及前后台恢复、安卓原生软键盘仍未全部闭环，综合目标保持进行中。

## 历史 0.5.4（2026-09-14）

普通注册账号的首页和大厅不显示开桌功能；仅管理员及获开桌授权的账号保留入口，撤权后立即消失。服务器也会拒绝越权开桌请求。

结算表新增四家准备/离线/托管状态和具体等待原因，修复短屏第四行局部遮挡、断线继续按钮与续桌后的迟到错误提示。连续测试还发现默认联机托管跳过合法胡牌，已修正为优先执行合法自摸/点炮，其他过牌和摸切策略保持。

最终逻辑/服务 131 项通过；前端最后逻辑修改后的 102 项界面回归全部通过。八局联机 717 秒通过，管理员仅一条最终记录、原房号续为四个空位；四局练习 181 秒通过。八局进程在自动胡补丁前启动且均流局，不代表新增分支覆盖；自动胡四种真实 WebSocket 情况已先复现失败、修复后通过。详见 CONTINUATION-0.5.4.md、experience-0.5.4-check.json。

SafeLink-212 当前发布 0.5.4-c58d1b9f，容器 healthy。更新前备份 SQLite，账号、开桌授权和战绩保留；公网版本、身份保护、原生 CORS 和 WebSocket 身份检查通过。

Ad Hoc 与安卓 APK 已导出并核验签名及资源；iOS build 14 已在 iPhone 17 Pro 模拟器安装启动。当前 Mac 锁屏，不能将这次命令启动写成原生触摸/软键盘/音频中断通过。物理 iPhone 未安装本版；Android 本轮仅构建与签名资源校验。两端包和源码位于 outputs。

综合体验目标本轮有进展，剩余原生键盘、音频中断与后台恢复尚未闭环，继续保留在 EXPERIENCE-AUDIT.md。风罚分、比下胡等未核准规则仍未启用。

## 历史 0.5.3（2026-09-14）

普通账号首页和牌桌大厅均不显示开桌入口，管理员与获授权账号保留入口。iOS 模拟器使用现有普通账号实际检查了首页、个人页和大厅，原生导航点击可用，真实牌桌与空位正常显示。

本版新增服务器校时，修正手机日期不准造成准备、出牌、累计超时和结算等待显示错误的问题。未准备玩家直接显示准备秒数；正常时间结束后显示累计余时和托管结果；断线时隐藏失准数字，恢复前台立即重发校时并丢弃睡眠前回复。详见 TIMING-0.5.3.md。

122 项逻辑/服务测试通过。完整 99 项界面回归在前台校时边界补丁前全部通过；补丁后的 10 项校时、联机首页和权限回归通过。中间一次运行出现无堆栈的 JSON 解析异常，同套用例无源码修改重跑通过，保留在验收记录中。浏览器分别偏移 −6 / +6 / −12 / +12 小时，四人仍可入座、准备和开局。证据见 experience-0.5.3-check.json。

SafeLink-212 当前为 0.5.3-41c06366，容器 healthy。更新前备份 SQLite，账号、权限与战绩保留；公网版本、匿名接口保护、原生 CORS 和 WebSocket 身份检查通过。

最终 iOS build 13 已安装启动于 iPhone 17 Pro 模拟器。Ad Hoc 严格签名校验通过，包含已登记的用户 iPhone；Android APK 与旧版测试签名一致，包内资源和当前构建一致。两端安装包、源码和普通账号截图位于 outputs。用户物理 iPhone 本轮不可用，未安装 0.5.3 到该手机；本轮未新增 Android 原生运行验证。

综合体验目标仍在进行。规则文档已修正暗杠可见范围、海底流局、桌费/本金、保米和计时的过时说明；风罚分、比下胡等未核准规则没有借本次改动启用。

## 历史 0.5.2（2026-09-14）

首页已改为联机优先：左侧进入牌桌大厅与房号入桌，右侧订阅真实房间和四个座位，支持点击空位直接入座。练习移到下方。普通注册账号首页、大厅均不显示开桌入口；仅管理员和获授权账号显示完整开桌设置，撤权后入口和设置弹窗即时关闭。

本版包含确认后的手牌整理、落牌、碰杠与补花反馈、恢复快照不回放历史音效、按配置生成规则说明、响应倒计时提示及撤权后取消自动续桌标记。详见 ONLINE-HOME-0.5.2.md。

113 项逻辑/服务测试通过。97 项界面用例完成回归：全量运行 96 项通过，最后一项手牌悬浮问题修正后，25 项相关回归有 24 项通过；其中发现的小屏碰牌提示与中央盘相交已调整，最终 4 项动作专项全部通过，已知失败均关闭。完整证据见 experience-0.5.2-check.json；未把一次失败运行写成全量通过。

SafeLink-212 已部署 0.5.2-f245e5a7，容器 healthy；更新前备份 SQLite，保留原账号、权限和战绩。公网健康版本、匿名接口保护、原生跨域和 WebSocket 身份检查通过。

iOS build 12 已安装启动于 iPhone 17 Pro 模拟器，实际显示当前普通账号、真实等待桌及四个空位，首页没有开桌按钮。原生界面点击检查遇到用户操作打断，因此不将该次点击计为通过；浏览器四客户端入座开局已验证。用户 iPhone 本轮检查为 unavailable，未安装 0.5.2 到物理手机。

Android 0.5.2 / versionCode 12 APK 编译并校验资源和旧版一致的测试签名，未新增 Android 模拟器运行声明。Ad Hoc IPA 已导出并通过 Apple Distribution 严格签名、横屏与资源一致性校验，描述文件包含用户已登记 iPhone。最新 APK、IPA、源码与首页截图位于 outputs。

综合体验目标仍在进行，完整清单见 EXPERIENCE-AUDIT.md；风罚分、比下胡等地方特殊规则仍需核准，不宣称完整荔枝规则或绝对公平。

## 历史 0.5.1

已交付开桌授权、安卓自适应图标、无偏安全随机数、暗杠信息隔离及胡牌专项审查；相关文档保留为历史证据。

## 历史 0.4.0

建桌大厅、准备、托管和续桌已实现。66 项逻辑/服务测试、71 项浏览器界面测试通过；最后的重连提示修正通过 9 项客户端专项检查。公网 212 服务为 0.4.0。iOS build 9 已在模拟器和用户 iPhone 17 Pro Max 安装启动；用户随后要求直接提供 APK：已完成 Android 0.4.0（versionCode 9）编译、签名检查及 Android 11 模拟器安装启动。源码仍保留 Windows 编译能力。微乐特殊计分尚未迁移。详细范围与证据见 [TABLE-LOBBY-0.4.0.md](TABLE-LOBBY-0.4.0.md)。

以下为历史版本记录。

2026-09-13，0.3.0 开发试打版。用户方向：iOS 优先、全程横屏、经典麻将游戏、大牌面与直接操作；参考微乐的粗笔大字与饱满牌面。用户已选定立体雕刻材质方向，42 种牌面与牌背已接入，并按反馈修正八条为上 W 下 M。后端已按用户确认部署到 SafeLink-212，原生客户端已配置该 HTTPS 地址，并安装打开于 iPhone 模拟器。完整 App 目标仍未全部完成。

## 历史 0.3.2 横屏修复

辅助页面、导航和弹窗适配左右刘海及底部 Home 指示条；牌桌菜单保持图案尺寸，触摸范围扩大；上方昵称不再遮住弃牌。客户端 0.3.2，iOS build 8，Android 源码 versionCode 8。后台接口未变，继续使用 SafeLink-212 的 0.3.1 服务。界面完整回归 44 项通过，最后修改的专项回归及三种安全区尺寸通过；连续四局真实计时 237 秒通过。见 LANDSCAPE-0.3.2.md。

追加花牌常驻展示和点牌后容器偏移修复，打包进 0.3.2 / iOS build 8。

微乐规则来源、iPhone 连接与原生触摸验收仍是剩余事项；本轮没有恢复安卓本机工作。

## 0.3.1 审查版本

52 项逻辑检查通过，包含压绝资格、逐笔收支、旧局兼容以及慢网确认。完整 39 项界面回归通过；最后短屏个人页/战绩/玩法的调整后，8 项相关检查再次通过（其中新增 2 项）。弹窗、表单、等待房、结算、个人页、大厅入口、长昵称与慢网反馈的修改见 REVIEW.md。

0.3.1 / iOS build 6 已完成两种 SDK 构建和真机签名校验；模拟器已安装启动，目视可见新版大厅入口。iPhone 仍未连接，模拟器坐标点击工具返回 noWindowsAvailable，尚未完成本轮原生触摸及听感验收。安卓只交付源码（versionCode 7），由用户在 Windows 构建。

最终规则依据已确定为微乐南京麻将，**规则迁移尚未完成**。公开官网未找到完整南京规则，已请求用户提供建房勾选项；需对照 App 内原始规则逐项核验。现有计分修复与账本通过自有玩法算例，不代表微乐平台一致性验证。

## 本轮 0.3.0

已修复先准备后补电脑无法开局，离线真人回桌及旧等待房恢复；手牌去除摸牌额外边距。新增背景音乐、落牌与碰杠胡等音效、独立开关及音量；重做图标和大厅标识，放大中央风位盘，增加当前玩家倒计时环，最后几秒变色提示。详情见 REFINEMENT-0.3.md。

逻辑测试完整 40 项通过，随后新增旧版本等待桌恢复回归 1 项通过（共 41 项）；界面完整 27 项通过，计时环底部位置修正后 3 种尺寸再次通过。后端 0.3.0 已部署并通过公网 4 真人和 2 真人加 2 电脑验收。iOS 模拟器已安装打开，真机包已完成开发签名；手机在本轮打包期间断开，目前等待重新连接，尚未把 0.3.0 装入该手机。

## 已实现和验证

- 全套 42 种原创雕刻牌面及翡翠牌背已用于实际游戏；使用原始图集的 CSS 视窗，不依赖字体。六条和八条为单独修正资产，八条每组两侧竖直、中间倾斜。整套预览见 `public/tile-catalog.html`，本轮材质与提示词见 TILE-STUDY.md，旧美术过程保留于 DESIGN.md。
- 明亮秦淮河大厅、四位原创头像、细纹绿绒桌布、风位指示、统一立体操作按钮、横屏全屏牌桌、单排手牌、连续点击同一张牌出牌、选择高亮、紧凑创建/加入表单和四人等待房间。
- iPhone 17 Pro / iOS 26.5 模拟器已安装当前新版 UI，实际显示新牌身、粗笔牌字、头像和桌布；选中并再次点击打出一筒后电脑继续，随后显示碰/过响应按钮。浏览器端也验证了点击出牌。
- 22 项浏览器检查通过，包含 7 种横屏尺寸、全部牌面加载和放大、创建/加入表单与等待房间、练习恢复、四客户端联机、房主转移、重连和解散。
- 实战布局新增六项回归：58 张弃牌、双方四组副露、碰牌响应均能完整显示；弃牌可直接点击放大，公开牌按四家切换，碰后手牌可继续出牌。
- 本轮更换雕刻素材后重跑 14 项视觉、横屏和实战布局检查，全部通过；边缘微调后牌组检查再次通过。
- 部署前再次完整通过 33 项引擎/服务测试与 22 项浏览器检查。源码在独立解压目录中执行 npm ci、生产编译通过；截图路径已改为相对项目目录。
- 修复旧材质对照页重复内嵌图片，页面从约 328 MiB 缩至约 4 KiB 外部引用，iOS 模拟器 App 从约 357 MiB 缩至约 29 MiB；牌面图片未压缩或降质。
- HTTPS/WSS 后端 `https://safelink.chat/mahjong` 已上线；独立 Docker 容器、持久卷、健康检查和自动重启已配置。四客户端公网联机及容器重启恢复验收通过，详见 DEPLOYMENT.md 与 deployment-check.json。
- 33 项引擎和真实服务测试通过，包含存储失败时操作回滚、稳定房主转移、真实服务重启恢复。
- 本机 100 个 WebSocket 客户端、25 个房间完成 100 局短时压测，0 个服务错误；延迟 P95 6.02 ms。详见 load-report.json；这是本机环回结果，不代表公网或长时容量。
- SQLite 持久化先于状态广播；iOS 模拟器构建通过。Android 工程保留并同步资产，当前优先完成苹果体验。

## 后续

用户最新决定：**安卓只准备源码，由用户自行在 Windows 编译；不继续本机安卓构建、工具链排查或模拟器验收。** 已补齐 `build-android.cmd` 和 WINDOWS-ANDROID.md；源码保留横屏、隐藏系统栏配置，Android 版本为 0.3.0 / versionCode 6。安卓模拟器已关闭。本轮本机 Gradle 在 JVM 初始化阶段失败，未生成新版 APK，不能算作安卓验收通过。

已按用户要求使用本机现有 Apple Development 签名，将联网版 0.2.0 安装并启动于其 iPhone 17 Pro Max（iOS 27.0 Beta）。设备已配对、开发者模式已开启，现有开发描述文件包含该设备，签名校验、真机安装和启动均成功。未向源码导出私钥或设备描述文件。

南京地方规则口径仍需定稿，未启用的比下胡、快照、天胡地胡等见 RULES.md。原生好友桌的实际触摸联机验收、跨网络真机测试、长时运行、运营管理和正式分发尚未完成。Android 最新资产与服务地址已准备，后续编译及原生安装验收由用户在 Windows 执行。

本地界面端口 5173，对局端口 8787。CodeGraph 已获授权并初始化。源码、开发工具与服务数据分开保存，源码包不包含真实服务数据、签名私钥和构建缓存。

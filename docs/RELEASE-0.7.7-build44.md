# 0.7.7 / build 44

## 改动

- 横屏安全区只移动受到灵动岛遮挡的头像、昵称、积分及相关提示，不缩小牌桌。切换左右横屏时重新计算位置。
- 牌局回放移除固定侧边留白和占用高度的控制区；画布占满窗口，控制条悬浮。播放中闲置 3 秒自动隐藏，点击画面切换显示，暂停时自动显示，操作进度或选择播放速度时保持可见。
- CSV 导出删除“把数”列；此前删除的“口径”行继续不显示。保留日期和“战队、会员账号、昵称、会员ID、桌数（8局/桌）、积分”。桌数按实际牌桌计，示例牌桌的 5 把仍计 1 桌，四人的积分仍为 -50、30、0、0。

## 验证

- 冻结 889 个源文件构建，两份由 Capacitor 生成的依赖配置仅因隔离构建目录而改变路径，规范化后与工作区一致。没有新增 Git 提交。
- 337 项单元/接口测试全部通过。首次与原生编译同时执行时 3 项测试超时；编译结束后使用 2 个工作进程重跑全部 337 项通过，未修改测试时限。
- 26 项相关 Chrome/WebKit 界面检查通过，覆盖回放、头像安全区与现有牌桌行为；实际构建产物的 4 项兼容检查通过，覆盖战绩、详情、回放及管理员功能加载。
- iPhone 17 Pro / iOS 26.5 模拟器左右横屏验证头像避让。使用隔离的模拟器宿主加载当前源码预览；并非在真机安装 build 44 的验收。正式安装包未配置本地预览地址。
- APK/IPA 均为 0.7.7 / 44，签名、版本、证书绑定及关闭调试检查通过，218 个网页资源与冻结构建产物逐字节一致。iOS 为沿用现有 4 台登记设备的 Ad Hoc 包。
- 牌桌仍按比例显示；非手机横屏比例的窗口可能有等比留边。此次回放修复的是容器固定边距和控制区挤压画面。

## 服务端

2026-09-16 20:28（北京时间）已部署 `0.7.7-build43-csv-tables-20260916`。导出修改无需更新客户端。7 项积分/战队测试在本地及 Linux 镜像通过，数据库副本校验通过，正式库原账目及示例牌桌快照保持一致；公网健康、权限、原生跨域和 WebSocket 鉴权检查通过。

详情见 `release-0.7.7-build44-verification.json`。安装包位于同级输出目录 `金陵麻将-0.7.7-build44`。

# 0.7.3 / build 39

2026-09-16 已推送完整本地源码、打包双端并部署服务器。构建源码提交：`6dd6c39cddbba5a72bdbac711c92e79620896415`。

## 本次内容

- 纳入其他任务完成的最新规则图核对、管理员战绩已读、登录键盘适配、听牌提示更新。
- 直排牌桌与手牌、花牌、副露；完整一至九筒素材和生成源图。
- 弃牌固定起点和容量，横向从左向右，换行从上向下，不再按数量重新居中。
- 可碰、杠、胡时突出实际目标牌及来源，按钮保留在自己手牌上方。

## 本轮验证

- macOS：32 个测试文件、306 项测试通过；TypeScript、Cocos、原生网页生产构建通过。
- Linux：Docker 构建通过；32 个测试文件、306 项测试通过。首次部署包缺少语音测试资料，补齐后重新完成构建和全量测试再切换。
- npm 依赖审计：0 已知漏洞。786 个源码及资源文件纳入快照，签名密钥、环境文件、数据库和构建缓存未提交。
- 实际 Cocos 页面检查第 10 张向下换行、旧牌不移动、六筒碰牌目标提示，显示与共享布局一致。
- Android：正式签名、版本号、16 KB 对齐、禁止调试/备份/明文通信、证书固定配置通过。
- iOS：Ad Hoc 签名、版本号、权限描述、证书固定配置通过；描述文件包含 4 台登记设备，有效期至 2027-05-13。
- Android 与 iOS 的 204 个内置网页文件逐一相同；服务器的 96 个 Cocos 文件与安装包逐一相同。
- 本轮没有在 Android/iOS 实体设备上重新跑长时联机或弱网测试；历史验收不算本轮实测。

## 服务端

`https://212.189.31.194/mahjong` 已更新到 `0.7.3-build39`，容器健康。切换前确认无人在线、无进行中牌局。用数据库副本验证启动后，最终切换并核对账号、头像、战绩、房间、战队等表行数保持。备份：`/opt/jinling-mahjong/database-backups/0.7.3-build39-final.sqlite`；原 `0.7.0-build36` 目录与镜像保留。

公网健康版本、匿名接口拒绝、原生 CORS、WSS 鉴权、管理员战绩已读接口匿名拒绝均通过。

规则及仍缺依据的范围见 [规则图核对](规则图核对-20260916.md) 和 [规则说明](RULES.md)。测试通过不代表穷举全部地方玩法。安装包校验结果见 [验收记录](verification-0.7.3/package-check.json)。

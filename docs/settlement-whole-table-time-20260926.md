# 统一整桌结束时间

2026-09-26修正管理员积分统计、CSV及Telegram自动日结的日期归属：由逐把 `point_records.at` 改为对应已结束牌桌的 `match_records.at`。

- 跨零点不拆桌；尚未结束的桌不进入已结束桌统计。
- 金额仍由有效逐把流水及首把桌费计算；不改写原始流水、战绩或账户。管理员沿用保存的本桌除数，Telegram日结固定原始净分÷2，周结仍为完成桌数×3。
- 使用EXISTS和最新终桌快照，防止遗留重复match记录乘大积分。保留原有练习、体验、未完成把过滤及桌费结转规则。
- 只有旧逐把流水、没有整桌结束证据的记录保留在原表，不猜测结束日期纳入汇总。明确清理过的历史流水不自动恢复。
- 管理员历史战队归属保持原逐把记录规则；Telegram按统计期结束时最终战队归属，未改战队算法。
- 每日查询桌数不能再因跨零点拆分重复出现；查询多天仍按game_id去重。

回归覆盖701439七把在前一天、第八把为0的情况，以及418990第一把2.5、后五把55的情况。验证原始point_records逐行不变、跨两天总额一致、CSV/管理员/日结一致、未结束桌不结算、重复终局不重计。

旧自动报表持久化快照保持不变。本次发现此前9月21–24日仅补发了整桌修正版文件，定时运行代码仍旧；更换未来计算函数不会自动更正已发送文件。历史修正须保留原文件及差异清单并明确标记补发，不删除或重写发送记录。

## 验证及当前发布状态

- 本地及Linux候选均100文件1381项测试通过。
- 候选镜像 `jinling-mahjong:0.8.3-whole-table-20260926` 对生产数据库只读重算9月25日日结，与独立终局快照审计逐人一致：23位有效会员，金额合计-536，终局一致性异常0。
- 2026-09-26 13:03独立Telegram定时任务已切换到该镜像。运行中容器再次只读重算得到同样结果，健康且重启次数0。
- Telegram状态库切换前后两次备份；历史文件、发送回执、排期内容指纹未变化。本次没有向群发送新消息或文件。
- 13:03主服务部署前发现372655桌正在进行，保护检查阻止切换；当时只更新独立报表任务，App管理员积分统计仍待上线。17:32已完成下述主服务切换。
- 主服务专用切换脚本（不重启已更新报表）：`output/deploy-whole-table-20260926/activate-main.sh`。候选已就绪；未自动安排后台监控任务。
- 日结原发送快照：2026-09-26 00:00:01.914，消息35，SHA-256 `4b09a25b93a391e149772edc5d4b53596c056835544820821d7bc6da136ac118`，原金额-543.5，7位会员需按整桌口径调整，净差+7.5。
- 已生成本地整桌修正版供确认，保留原文件版式及金额排序；未覆盖原件、未补发Telegram。

## 后续完成记录

- 红色标注更正版已于13:30发送到原Telegram群，消息36，原消息35及定时发送快照未改写。回执保存在 `outputs/daily-whole-table-20260926/telegram-redline-delivery-receipt.json`。
- 用户要求最后一桌结束后立即部署，自动跟进确认无进行中对局后，于17:32切换主服务为 `jinling-mahjong:0.8.3-whole-table-20260926`。网页/源码current已同步切换，报表、网关及安装包服务未重启。
- 两次备份均无活动牌局，SHA-256一致：`d946c40b7cbcfa26936a0efcc112c949c9076a210a8c31b8dc4fc4f1a1f58676`。路径 `/opt/jinling-mahjong/database-backups/0.8.3-whole-table-20260926/main-cutover-20260926T093243Z/stopped/final-backup.sqlite`。
- 切换后数据库完整性、账单数据及强更策略指纹不变。主服务与报表服务均健康、重启次数0。
- 公网网页资源与候选清单逐项匹配，健康接口0.8.3；积分统计、CSV及会员对局接口匿名访问均401。
- 使用运行中主服务的实际计算代码，在生产数据库的只读来源/可丢弃副本上核对：孙翠华9月25日9桌、61把、131.5；依依9月25日0桌、0分，418990桌终局57.5；CSV包含正确金额及整桌结束时间说明。未创建生产登录或测试牌桌。
- 此次为服务端查询逻辑更新，不需重打APK/IPA，用户重新查询即可。自动跟进完成后删除。

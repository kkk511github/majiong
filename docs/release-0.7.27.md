# 金陵麻将 0.7.27 / build 64

牌桌只保留南京话短句，移除实时录音发送；修复注册和首次设置密码后返回登录时的键盘遮挡。

- 删除语音按钮、按住说话面板、录音采集模块入口及相关界面样式。保留12条南京话短句及男女声共24段录音，设置项改为“短句语音播放”。
- Android不再声明RECORD_AUDIO，iOS不再包含NSMicrophoneUsageDescription。背景音乐、报牌、短句播放及音频路由保持。
- 账号表单提交时保持输入框焦点，防止按钮被禁用后焦点落到BODY、键盘未收起而表单恢复为高布局。
- 登录、注册及首次设置密码切换时清理旧焦点与表单/外壳滚动；在React恢复旧输入焦点之后再次结束旧编辑状态。
- 不报告可视高度的覆盖式键盘使用顶部紧凑表单，当前输入行自动滚入可见区。实际resize及iOS可视区域仍根据实时高度适配。

## 验证

- 54项针对性单元/服务端测试通过。
- 短句单入口、录音移除与男女声播放Chrome/WebKit共14项通过，单入口布局7项通过。
- 键盘Chrome/WebKit共26项定向回归通过，覆盖本地真实注册、首次改密码、退出重登、100px覆盖式区域、90px可视区域、反复开关和显隐。
- Android正式签名与无麦克风权限、iOS Ad Hoc签名与无麦克风用途键，以及两平台全部257项网页资源一致均核验通过。具体结果见build64验证文件。
- 原生模拟器只检查无生产提交的登录/注册键盘流程；覆盖式输入法由浏览器遮罩与DOM命中测试复现。本轮未在用户的实体手机上测试。

## 范围

本次不修改计分规则、历史记录或分发平台当前安装包。另对房间498046第3局进行了只读核查：全球独钓来自现有三嘴快照候选，实际三组落地；细节见`output/room-498046-audit/REPORT.md`。本版不代表该快照规则已变更。

iOS仍先交付用户企业重签，收到签名回传后才能上传分发。没有自动送签或上传本地IPA。

网页版 `/play/` 已同步发布；游戏服务未重启。Android 11/API30、WebView83、LatinIME模拟器的7阶段操作中，5次键盘显示检查通过，密码及提交按钮始终位于IME上方；未向生产提交注册或登录。

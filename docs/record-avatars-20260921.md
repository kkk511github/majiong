# 战绩与回放头像修复

2026-09-21：战绩列表、详情顶部和牌局回放现在使用账号当前保存的头像，未设置或图片加载失败时保留默认图。历史记录通过原始账号 ID 对应当前头像，不改写战绩和积分。

隐私桌遵循原有匿名逻辑：普通成员的战绩隐藏其他人的头像；公开回放隐藏所有头像，避免通过图片识别被隐藏的成员。

服务器 `212.189.31.46` 已部署 `jinling-mahjong:record-avatars-20260921`，健康检查通过。仅升级头像数据接口，管理后台与统计服务保持原部署。服务器两个业务文件的 SHA-256 与工作区一致。

已将当前代码编译为 iOS Simulator App，安装在 iPhone 17 Pro 模拟器，使用正式 HTTPS 地址。房间 218313 的真实战绩与第 1 把回放已经人工查看，均显示成员上传的头像。截图保存在 `output/record-avatars-live-ios.png` 和 `output/replay-avatars-live-ios.png`。此前临时演示服务已停止，正式模拟器包不含演示数据和登录脚本。

验证：战绩改动后的全量单元测试 1030 项通过；追加回放头像后，战绩权限/回放单元测试 3 项通过；头像列表与 Cocos 回放浏览器测试 Chrome、WebKit 共 4 项通过；TypeScript、原生资源构建及模拟器编译通过。

手机上已有的正式安装包不会因服务端升级自动获得新的头像组件；本次已安装的是模拟器构建，尚未导出新的真机 APK/IPA。iOS 真机包仍遵循先交付用户签名、收到签名包再上传的流程。

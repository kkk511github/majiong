# 验证记录

## 当前 0.4.0

建桌大厅、准备、托管和续桌已实现。66 项逻辑/服务测试、71 项浏览器界面测试通过；最后的重连提示修正通过 9 项客户端专项检查。公网 212 服务为 0.4.0。iOS build 9 已在模拟器和用户 iPhone 17 Pro Max 安装启动；用户随后要求直接提供 APK：已完成 Android 0.4.0（versionCode 9）编译、签名检查及 Android 11 模拟器安装启动。源码仍保留 Windows 编译能力。微乐特殊计分尚未迁移。详细范围与证据见 [TABLE-LOBBY-0.4.0.md](TABLE-LOBBY-0.4.0.md)。

以下为历史版本记录。

日期：2026-09-13。当前客户端：0.3.2（iOS build 8；Android 源码 versionCode 8）；公网后端仍为兼容的 0.3.1。

## 0.3.2 横屏追加验证

完整 44 项界面回归通过；头像位置调整后 8 项相关检查通过，随后 844 / 874 / 932 三种 CSS 安全区专项检查共 4 项通过。菜单点击范围至少 44px，导航避开底部 21px 手势区，菜单扩大区域边缘可点击。真实计时连续完成四局，237 秒、无页面异常、四局收支一致并保存战绩，报告见 continuous-play-0.3.2.json。该检查不代替真实 iOS 触摸、软键盘、听感或微乐算分一致性验证。详细修复见 LANDSCAPE-0.3.2.md。

本轮业务逻辑未改变，沿用下方 52 项逻辑检查证据。安卓没有重新在本机编译。

## 0.3.1 全面审查进展

- TypeScript / native 生产构建通过；52 项逻辑检查通过，100 个种子的账本逐家对账通过。
- 完整 39 项浏览器检查通过。随后短屏个人页裁切、战绩和玩法页调整后，8 项相关检查通过；新增了长昵称、慢网准备防连点、新练习和昵称保存覆盖。
- iOS 模拟器与真机 SDK 构建通过，开发签名严格校验通过。两个包均为 0.3.1 build 6、只支持横屏、内置公网后端地址；iPad 全屏声明已补齐。
- 已安装启动于 iPhone 17 Pro 模拟器，并目视核对新版大厅。原生点击工具仍返回 noWindowsAvailable，未完成本轮原生触摸和听感测试；iPhone 17 Pro Max 仍处于 unavailable，未安装本轮更新。
- SafeLink-212 0.3.1 公网四真人与两真人先准备后补陪练的创建/加入/开局、手牌私密、出牌同步、重连与解散退出均通过。
- Android 未在本机重新编译，Windows 构建由用户执行。
- 微乐规则一致性尚未验证，需要 App 内完整规则与建房选项。当前可运行 profile 仍是 nj-casual-v1。

## 0.3.0 历史验证（iOS build 5；Android 源码 versionCode 6）

安卓交付调整：用户要求仅准备源码，自行在 Windows 编译。已加入 Android 横屏及系统栏隐藏配置、Windows 构建脚本和说明。前端资产同步成功，但本机 Gradle 在 JVM 初始化阶段失败，未完成本轮安卓编译和原生运行验证；已停止安卓模拟器及本机编译工作。Windows 脚本未在 Windows 环境执行。

本轮：TypeScript 与 native 构建通过；完整 40 项逻辑测试及新增旧等待桌恢复 1 项通过；完整 27 项浏览器检查通过，随后倒计时环底部位置修正后 568/874/932 三种尺寸再次通过。新增检查覆盖：先准备后补陪练、摸牌处于排序中间及出牌后的手牌间距、计时环不越界、实际 Web Audio 输出振幅、音乐和音效独立静音、音量持久化、后台挂起与恢复。iOS 两种 SDK 编译通过，真机签名校验通过；模拟器已安装并目视确认大厅新图标。用户接管模拟器后停止点击，因此不声称完成了本轮原生好友桌的完整触摸验收。

后端已上线 0.3.0，公网四真人与两真人先准备再补电脑的联机、出牌、重连、手牌私密和解散退出均通过。手机断开连接，0.3.0 真机包已签名待安装；之前 0.2.0 安装回执保留。

以下是 0.2.0 历史验证：

| 范围                      | 结果                                                                                                                                   |
| ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| TypeScript 与生产构建     | npm run build 通过                                                                                                                     |
| 引擎与真实 WebSocket 服务 | 33 项通过；含真实重启恢复、数据库写入/删除失败回滚与重试、房主转移                                                                     |
| 完整随机对局              | 100 个种子通过；检查牌的唯一性、积分守恒与手牌保密                                                                                     |
| 浏览器完整流程与布局      | 22 项通过；含 7 种横屏尺寸、全套 42 种牌与牌背加载、放大预览、练习点击出牌、四个独立客户端联机                                         |
| iOS 模拟器编译            | Xcode 26.6 / iOS 26.5，BUILD SUCCEEDED                                                                                                 |
| iOS 当前原生界面          | iPhone 17 Pro 横屏已安装本轮微乐风格 UI，显示新牌身、字形、头像与桌布；选中一筒，再次点击后该牌进入弃牌区，电脑继续并出现碰/过响应按钮 |
| 实战布局追加验证          | 6 项通过；真实引擎残局含 58 张弃牌、双方四组副露和碰牌响应，覆盖 568 / 667 / 844 / 932 横屏；公开牌逐家数量与顺序核对通过              |
| iOS 界面截图              | outputs/ios-0.2-landscape-lobby.png 与 outputs/ios-0.2-landscape-table.png，后者包含碰/过响应状态                                      |
| 本机并发                  | 100 客户端、25 房间、100 完成牌局，0 服务错误，P95 6.02 ms；见 load-report.json                                                        |
| Android                   | 旧版 Debug APK 构建通过；新资产已同步，最新版本原生安装与触摸验收暂缓，用户要求先做苹果                                                |

## 限制

本轮雕刻牌面更新：生产编译、iOS 模拟器构建通过，并已重新安装启动。重跑 14 项牌组、7 种横屏、残局和碰牌检查全部通过；牌身边缘微调后再次通过牌组加载与八条放大预览检查。八条单独核对为上 W、下 M，每组两边竖直、中间倾斜。iPhone 17 Pro 原生保存牌局中的八条碰牌已显示新版；进入「公开牌一览」检查本人弃牌、八条副露和花牌，再返回牌桌正常。原生截图 `outputs/ios-0.2-landscape-public-tiles.png` 与更新后的 `outputs/ios-0.2-landscape-table.png`。本轮未重复未改动的引擎和联网测试，上表这些结果为之前的验证记录。

随后部署验证：33 项引擎/服务测试、22 项浏览器检查再次全部通过。独立解压源码包 npm ci 与生产构建通过；材质对照页已修正重复嵌入并目视检查。后端已部署到 SafeLink-212；4 个真实公网 WSS 客户端完成创建/加入/开局/出牌/重连、容器重启恢复与解散退出，结果见 deployment-check.json。联网版 iOS 构建、安装、启动和横屏显示通过；已核对安装包包含 `https://safelink.chat/mahjong`。用户接管模拟器后停止操作，未把 Node WSS 验证表述为原生好友桌触摸联机验证。

随后真机安装：用户要求安装到其 iPhone。已发现一台有线连接并配对的 iPhone 17 Pro Max（iOS 27.0 Beta），开发者模式开启；使用本机已有 Apple Development 身份及覆盖该设备的通配开发描述文件完成签名。Xcode 真机构建 BUILD SUCCEEDED，codesign 深度校验通过，devicectl 安装 `com.jinling.mahjong` 成功并报告启动成功。安装包含当前横屏界面、雕刻牌面与 SafeLink-212 HTTPS 后端地址。详细安装回执仅存本机 work/，未将设备标识或描述文件打入源码交付物。

已完成开发签名真机安装，尚未完成 TestFlight 或商店分发。公网短时联机验证不等同于原生好友桌完整交互、跨运营商网络、弱网和长时测试。具体规则边界见 RULES.md。

原生构建结果：iOS 模拟器与真机 SDK 编译、开发签名严格验证通过，均为 0.3.2 build 7；只支持横屏。模拟器已安装启动，安装包的 JS/CSS 与构建产物逐文件一致。已目视新版对局和个人页；取消托管后出现手动出牌状态。随后模拟器被用户切换，自动坐标点击再次返回 noWindowsAvailable，未完成本轮原生软键盘、摸打整套操作或听感验收。iPhone 17 Pro Max 最新设备枚举仍为 unavailable。

花牌常驻及点牌偏移的最终版本完整通过 66 项界面检查（49.3 秒）。新增 20 个花牌布局场景覆盖四家分布、任一玩家持有 20 花、568/844/874/932 横屏及刘海安全区；手牌回归同时检查花牌可见、容器偏移为零。两种 iOS SDK 重新编译通过，版本 0.3.2 build 8。

0.3.2 build 8 已安装并启动于 iPhone 17 Pro 模拟器，目视确认真实练习第 3 局四家花牌常驻可见，手牌与操作保持完整。已校验安装目录及真机包 JS/CSS 与本轮构建逐文件一致。真机开发签名严格校验通过；iPhone 17 Pro Max 仍为 unavailable，尚未安装此版到真机。

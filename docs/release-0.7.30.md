# 金陵麻将 0.7.30 / build 67

修复安卓点击更新时立即提示“安装包校验信息无效，请重新检查版本”的问题。旧插件通过 Capacitor 的 `getLong` 读取大小，但 Android JSON 将 1 GiB 以内的正常文件大小解析成 `Integer`，导致读到空值。

新插件接收有效的数值整数并转换成 long，拒绝缺失、非数值、小数、非有限值、非正数及超过 1 GiB 的值。原有固定 HTTPS 来源、文件大小、SHA-256、应用标识、构建号、最低系统版本及签名证书校验继续执行。

Android 包修复原生更新模块，沿用 build66 的 257 个已验证网页资源。iOS 按当前源码重新构建为 0.7.30 / 67，公共工程版本同步为 0.7.30。计分和牌桌逻辑沿用当前版本。两家归零结束提示只完成设计预览，尚未加入安装包。

已经遇到该错误的设备，需要从固定分发页手动下载修复版 APK 并覆盖安装一次，不必卸载。旧设备的原生插件不能靠服务器元数据更新而修复。修复后，今后的应用内下载更新仍需用户在系统安装窗口确认。

固定入口：https://212.189.31.46/app/jinling-mahjong?platform=android

验证：11项JS更新契约测试、4项真实Android JSON/Capacitor测试通过。专用Android11/API30模拟器使用包含相同修复的隔离测试65，真实WebView桥下载线上66，完成TLS、大小、SHA、包名、版本、签名校验、安装来源授权和系统覆盖安装；安装后版本正确，UID与测试数据保留。联网测试只存在本地output目录，不包含在默认测试或正式APK中。

正式67 Release构建、原签名、16KB对齐检查通过，257项网页资源逐项匹配66快照。构建产物与具体发布状态见本版本验收记录。

Android67已通过现有分发服务原子替换流程发布，保持固定下载入口与应用ID。游戏服务及历史记录未改动。旧Android包保留在本地私有备份中供回退使用。

iOS已重新archive/export，按用户要求交付为`unsign-0.7.30-build67.ipa`。包内版本0.7.30、Build67，257项冻结网页资源、96项Cocos资源与24条短句音频核验通过。文件名unsign为用户指定名称；该导出仍带有效Ad Hoc签名，供用户企业重新签名，不表示已经无签名。原始导出验证见`ios-0.7.30-build67-verification.json`。

用户回传的`金陵麻将 (3).ipa`当前已在原固定入口提供，公开完整下载与其原件SHA一致，manifest为Build67，Android67保持不变。回传包保留签名方与旧版相同的联网授权组件。新旧重签包均未通过本机严格codesign检查，且有描述文件应用标识不匹配告警；未在用户iPhone安装，不能将分发元数据可读取等同于签名有效或安装成功。发布验收见`ios-0.7.30-build67-publication.json`。

# 公告已读确认（2026-09-24 已上线）

部署结果见 [0.7.37 部署记录](deployment-0.7.37-20260924.md)。以下末段记录的是实现完成时的暂停状态，已由随后用户明确要求开始部署而解除。

后台公告列表新增本版「已读 / 总人数」及未读人数。点击进入详情可按已读/未读/全部筛选，按昵称、账号、玩家ID搜索，20 人分页，并查看首次成功确认时间和手动刷新。

- 复用已有 `announcement_reads`：确认接口由登录会话确定账号，跨设备重复确认不重复计数、不覆盖首次确认时间。管理员查看名单不会代用户确认。
- 按发布版本统计；仅改草稿不会重置，重新发布后新版重新确认。撤回仍显示最后发布版本的阅读记录；查询旧版本遇到重新发布返回409，防止混用新旧人数。
- 人数基于当前可登录账号（含管理员）；停用、删除、必须先设置初始密码的账号不计入。新增账号计为未读，恢复账号可重新计入原回执。这不是发布时成员快照。
- 仅管理员可查询统计和名单。未登录401，普通成员403；名单不返回密码、令牌等字段。
- 新增复合索引优化按公告、版本的统计；没有改写历史确认记录或玩家确认流程。
- 全量1298项单元测试、11项后台浏览器用例及后台构建通过（包含同期报表昵称改动）。

用户之前暂停了主服务部署，因此本次不重启生产牌桌服务，不创建测试公告或伪造真实用户已读记录。待下次获准上线时必须重新构建主服务和后台：之前暂存的 `0.7.37-build75-20260923` 候选镜像/后台包早于这次已读统计，不能直接当成包含本功能的最终版本。

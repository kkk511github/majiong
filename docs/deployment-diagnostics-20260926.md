# 2026-09-26 服务端、后台及网页部署

北京时间03:11完成切换，03:12–03:13完成公网检查。

后续：0.8.3 / Build80客户端已打包交付，见 [安装包记录](release-0.8.3-build80.md)。服务端保持0.8.2；下文“尚未交付”为部署当时状态，不表示缺少后续安装包。

- 主服务镜像：`jinling-mahjong:0.8.2-diagnostics-20260926`，健康、重启次数0。
- 本地最新修改以显式文件清单和SHA-256冻结；未要求提交或推送Git，本次未自动commit/push。源码current指向 `/opt/jinling-mahjong/releases/0.8.2-diagnostics-20260926/source`。
- `/play/`、`/manage/`同步切换并保留旧散列资源，安装包及Telegram报表服务、网关的容器ID/启动时间/重启次数均未变化。
- 上线内容包括会员日期区间对局查询、iOS/安卓诊断接收和后台采集、用户主动上传接口、禁赛后原桌打完保护，以及新收桌记录的结束时间修正。
- `persist`为缺少finishedAt的新结束牌桌设置实际持久化时间，覆盖closeTable绕过publish的路径；已有finishedAt保持原值，不重写历史战绩日期。新增两个服务端回归验证跨期收桌与已完成桌再收桌。
- 网页牌桌包含Canvas圆角兼容修复。公网WebKit移除roundRect后实际加载ready=true、39个模型、无页面错误；这不是旧iOS系统实测。

## 数据安全与验证

切换前无playing/claiming/ended对局，仅2张等待桌。先在线备份，再停服后二次备份，两份SHA-256一致：

`a696d5d0cfd7a686e991103d936b44b0edbc79f0dffda8975116e2885f7818a1`

备份目录：`/opt/jinling-mahjong/database-backups/0.8.2-diagnostics-20260926/cutover-20260925T191137Z/stopped/final-backup.sqlite`。

账号、战队、战绩、回放、积分、公告及已读数据、强制更新策略内容哈希保持一致，数据库完整性检查ok。没有提高强更最低版本，没有修改会员参赛状态。保留切换前服务日志及回滚配置。

本地与Linux候选最终均99文件1378项单元测试通过；网页366文件、后台6文件及源码覆盖清单逐文件校验通过。公网入口JS/CSS、Cocos入口与产物哈希一致，健康接口版本0.8.2，会员查询、诊断采集、用户上传接口匿名访问均401。

首次候选失败原因：Mac AppleDouble元数据误入测试、Cocos辅助函数测试读取未生成的Creator临时tsconfig。重新打包禁用扩展属性；保留原测试范围与15秒超时，设置esbuild原始JSON字符串配置避免读取Creator配置。未跳过业务测试，第三次候选全量通过才上线。过程日志均保留。

## 清理

仅清理超过24小时未使用的Docker构建缓存，保留8GB缓存预算。Docker报告回收37.62GB（包含共享层计量）。部署全部候选后，实际磁盘从117GB盘使用86GB/74%降至65GB/56%，可用52GB。

内存可用14708MB，无内存压力，没有杀进程或执行drop_caches。数据库、日志、安装包、运行镜像和带标签回滚镜像、数据库备份均保留。

## 尚未交付的客户端部分

未生成或发布新的IPA/APK。旧0.8.0 IPA没有绘图兼容修复及新诊断插件；用户上传入口和iOS原生采集需新安装包。iOS15以下的最低目标仍未确认，当前原生框架最低15.0，不能宣称支持14/13。

## 回滚

使用切换备份父目录的server.env和previous-links.txt恢复主服务镜像、网页/后台/源码链接。只重建server项目的mahjong服务，不重启报表、安装包或网关。**不要把旧数据库覆盖到运行中的新数据上**；新增诊断表与查询索引可保留供旧代码忽略。

本地验证材料：`output/deploy-diagnostics-20260926/`。生产候选与部署日志位于同名服务器发布目录。

# 0.7.4 / build 40

2026-09-16 已发布。构建源码提交：`dc3d4d9`。

## 修改

上家弃牌从上往下，下家弃牌从下往上；每列固定起点和容量，换列时旧牌不移动。自己与对家的横向顺序、向下换行保持不变。iOS、Android、服务器共用同一 Cocos 场景。

## 验证

- 本地和 Linux 均为 32 个测试文件、306 项测试通过，包含四种观看座位及换列边界。
- TypeScript、Cocos 运行包、原生网页与服务器生产构建通过。
- Android 正式签名、版本号、16 KB 对齐和原有安全配置通过。
- iOS Ad Hoc 签名、版本号和权限配置通过，支持描述文件登记的 4 台设备。
- 两端 204 个内置网页文件逐一相同；服务器 96 个 Cocos 文件与安装包逐一相同。
- 本次没有在两端实体设备上重新跑长时或弱网测试。

## 部署

服务 `https://212.189.31.194/mahjong` 已更新为 `0.7.4-build40`，容器健康。切换前无在线玩家、无进行中牌局；数据库副本启动和切换前后数据行数检查通过。数据库备份：`/opt/jinling-mahjong/database-backups/0.7.4-build40-final.sqlite`。保留 `0.7.3-build39` 供回退。

公网版本、匿名接口保护、原生 CORS、WSS 鉴权验证通过。部署时逐项校验了 630 个服务端源码和资源文件。

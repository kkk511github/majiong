# 0.7.37 主服务及后台上线（2026-09-24）

## 已上线

生产地址 `https://212.189.31.46/mahjong`，主服务镜像 `jinling-mahjong:0.7.37-build75-20260924`。

- 在线邀请、接受/拒绝、服务端入座校验。
- 后台最低版本和强制更新开关；进行中旧桌/原席位保护，断线重连可打完整桌，终局后检查更新。
- 公告已读人数、未读人数、名单、确认时间、筛选、分页及版本隔离。
- 一并包含此前确认的桌面展示、开局确认能力兼容等主分支修改。胡牌规则和计分文件与更新前线上文件哈希一致。

该次明确部署授权覆盖此前用户暂停的主服务更新。重新构建了包含最新公告统计的候选镜像，未直接使用前一天已过期的候选包。

## 切换与数据安全

- 切换前两次检查均无进行中对局；最初检查无在线真人。
- 先在线一致性备份，停止主服务后再次检查无新增进行中对局并制作最终备份；SQLite integrity_check 为 ok。
- 备份：`/opt/jinling-mahjong/database-backups/0.7.37-build75-20260924/cutover-20260923T175208Z/stopped/final-backup.sqlite`。
- 备份 SHA-256：`15d2d3903f19992ebbe0f105819420714c196c82ab69072d4e57948bf6991e39`。
- 切换前后账号、战队归属、单局/整桌战绩、积分流水、历史费用结转，以及公告内容/已读回执/审计/请求记录逐表行数与内容哈希一致。
- 新强更设置为 `enabled=false, minimumVersion="", revision=0`，未开启强更、未手工修改任何已读记录。
- 主服务于 `2026-09-23T17:52:10.945Z`（北京时间9月24日01:52）启动，健康，重启计数0。
- Telegram报表 `reports-nickname-20260924`、安装包服务、网关的容器ID、镜像、启动时间和重启计数均保持不变。
- 没有上传/替换APK或IPA，没有推送Git远端；此前交付的0.7.37 / Build75安装包不变。

## 验证

- 当前代码此前全量1298项测试通过；本次生产候选镜像6组/91项核心回归通过（含邀请、旧版/新版混用、强更实时开关和完整牌局保护、后台公告统计、建桌/开局）。
- 网页272个资源、后台6个资源与本地新构建逐文件匹配；保留旧散列资源，减少旧页面懒加载中断。
- 公网严格TLS健康检查返回0.7.37；匿名API鉴权、原生CORS、匿名WebSocket拒绝检查通过。
- 公网网页/后台入口及JS/CSS资源哈希匹配；强更设置、公告列表/已读名单接口匿名访问均被拒绝。
- 线上关键服务源码哈希与本地一致；公告回执索引已存在。
- 没有创建生产测试账号、发送生产邀请/公告，或伪造真实用户阅读确认。真实收发场景在隔离候选环境验证。

网页路径 `/play/`、后台路径 `/manage/` 均切换到 `0.7.37-build75-20260924`。服务器准备/验证材料位于 `/opt/jinling-mahjong/releases/0.7.37-build75-20260924/`；本地公网验证记录为 `output/deploy-0.7.37-20260924/public-verification.json`。

## 回滚

先确认维护窗口并检查进行中牌局。恢复上述备份目录的 `server.env`，仅对 `server` Compose项目的 `mahjong` 服务执行 `up -d --no-deps --no-build --wait`；按 `previous-links.txt` 恢复网页、后台、源码软链接。不要恢复旧数据库覆盖新产生的对局/已读记录；不要重启Telegram、网关或安装包服务。
